<UserControl x:Class="FishComponents.Fish"
             Name="UCFish"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" SizeChanged="UserControl_SizeChanged" Height="300" Width="300">
    <UserControl.Resources>

        <TranslateTransform x:Key="EllipseTransform" X="-1.5" Y="-1.5"/>
        <FrameworkElement x:Key="EllipseSize" Width="3" Height="3" />
    </UserControl.Resources>
    <Canvas Background="Aqua" Name="p">
        <Ellipse Name="CenterP" Width="{Binding Source={StaticResource EllipseSize}, Path=Width}" Height="{Binding Source={StaticResource EllipseSize}, Path=Height}" 
                 Stroke="Red" 
                
                 Canvas.Top="{Binding ElementName=UCFish, Path=Center}">
            <Ellipse.RenderTransform>
                <TransformGroup>
                    <Binding Source="Source={StaticResource EllipseTransform}"/>
                        
                    </TransformGroup>
                
                    
                </Binding>
            </Ellipse.RenderTransform>
            
        </Ellipse>
    </Canvas>
    
</UserControl>
